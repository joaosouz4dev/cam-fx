# CamFX - dependencias de runtime
# Segmentacao de pessoa via ONNX Runtime com GPU (DirectML) e fallback CPU.
onnxruntime-directml==1.24.4
# MediaPipe (apenas para o auto-framing / Face Detector).
mediapipe==0.10.18
# Processamento de imagem (blur, composicao, redimensionamento)
opencv-python==4.10.0.84
numpy==1.26.4
# A camera virtual usa o driver proprio CamFX (driver/CamFXSource.dll),
# alimentado por memoria compartilhada em camfx/virtualcam.py. Sem dependencia
# externa de runtime para isso.
# Interface (janela WebView2 com HTML/CSS/JS)
pywebview==6.2.1
# Bandeja do sistema (icone, menu, iniciar minimizado)
pystray==0.19.5
Pillow==10.4.0
# Enumeracao de cameras por nome (DirectShow) para filtrar a camera virtual
pygrabber==0.2
# Troca de rosto (face swap): deteccao/alinhamento/recognition (buffalo_l) +
# inswapper. ATENCAO: o modelo inswapper_128 e licenciado apenas para pesquisa/
# uso nao comercial (ver camfx/terms.py). Mantido so no branch feat/face-swap.
# insightface puxa onnxruntime como dep; instalamos com --no-deps no build do
# branch para nao conflitar com onnxruntime-directml, e adicionamos as libs
# transitivas necessarias explicitamente.
insightface==0.7.3
onnx==1.16.2
scikit-image==0.24.0
# deps transitivas do insightface (instaladas explicitamente por causa do
# --no-deps). albumentations fixado em 1.4.x: a 2.x exige numpy>=2, que quebra
# o mediapipe (numpy<2). O detector buffalo_l roda em CPU (DirectML quebra o
# det_10g); so o inswapper usa GPU.
requests
easydict
prettytable
tqdm
scikit-learn
lazy_loader
networkx
tifffile
imageio
albumentations==1.4.3
